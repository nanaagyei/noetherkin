# Synthetic PetClinic workspace

Every structured record is `data_class: fixture`. The learner, timestamps, completed task, verification and review decisions are invented demonstration data. No learner work, test run, source checkout, deployment or upstream contribution occurred. The real upstream repository is cited only as project context.

The directory above `.apprenticeship` is the example workspace root. `source_path: source` and its revision are fixture bindings, not an assertion that a checkout exists. This is the only fixture exemption from live source-binding existence checks; schema and reference checks still apply. The artifact URIs resolve to the included synthetic transcript and notes.

Read the completed TASK-map-demo alongside EVID-map-demo and its code/task reviews. TASK-failure-demo is assigned with no results. The baseline begins unassessed; a later technical assessment demonstrates one E0 competency within the fictional scenario. The performance review says insufficient-evidence and no promotion record exists. Thus effective level remains E0 without equating a finished task to advancement.

All nine schemas have example documents. Global and pinned project definitions are identical fixture catalog data; promoting a catalog candidate to live supported status requires independently inspected provenance and removal of fixture caveats, not changing a label alone.

These are protocol 2.0 synthetic fixtures. YAML-named files use the canonical JSON subset for consistent parsing. The later navigation assessment is explicitly longitudinal, while bounded design checkpoints are exercised in the freeze specimens. The complete reviewed 1.0 examples remain in the protocol archive.
